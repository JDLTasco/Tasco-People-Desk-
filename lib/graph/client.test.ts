// Moved to lib/email/html-to-text.test.ts (2026-09-23) -- emailHtmlToText
// itself moved out of this file to lib/email/html-to-text.ts so the ticket
// detail page and lib/archive/render.ts could share it without reaching
// into the Graph client module. Left as an inert placeholder rather than
// deleted -- the tool used to build this project cannot delete files in
// this environment (same convention as vitest.config.ts, see STATUS.md).
export {};
